---
const { src, alt, width = 600, height = 400, className = '', style = '' } = Astro.props;
---

<figure class={className} style={style}>
  <img src={src} alt={alt} width={width} height={height} loading="lazy" decoding="async" />
</figure>
