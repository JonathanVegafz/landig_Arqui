---
import logo from '../../assets/optimized/logo-hero.webp'
import { COMPANY_INFO } from '../../lib/constants-loader';
---

<section class="about section-bg-gray" id="about" aria-label="Sobre nosotros">
  <div class="container">
    <div class="about__content">
      <div class="about__image-wrapper">
        <figure class="about__img">
          <img 
            src={logo.src} 
            alt="Logo IG Construcciones" 
            loading="lazy"
            decoding="async"
            width="312"
            height="250"
            style="aspect-ratio: 312/250;"
          />
        </figure>
        <div class="about__image-overlay"></div>
      </div>
      
      <div class="about__text">
        <div class="about__header">
          <span class="about__subtitle">Sobre Nosotros</span>
          <h2 class="about__title">Encabezado por un equipo sólido de profesionales</h2>
        </div>
        
        <div class="about__description">
          <p class="about__paragraph">
            Dedicados al área del diseño y la construcción. Con casi dos décadas de trayectoria, hemos aprendido a escuchar e interpretar cada detalle de los deseos de nuestros clientes.
          </p>
          
          <p class="about__paragraph">
            No solo construimos viviendas, creamos hogares funcionales y eficientes, adaptados perfectamente al entorno y a tu estilo de vida. Nuestro compromiso es total: desde el primer boceto hasta la entrega final, garantizamos seguridad, cumplimiento de plazos y un estándar de calidad insuperable.
          </p>
        </div>
        
        <dl class="about__stats" role="list" aria-label="Estadísticas de la empresa">
          <dt class="stat">
            <span class="stat__number" aria-label={`Más de ${COMPANY_INFO.experience}`}>{COMPANY_INFO.experience}</span>
            <dd class="stat__label">Años de Experiencia</dd>
          </dt>
          <dt class="stat">
            <span class="stat__number" aria-label={`Más de ${COMPANY_INFO.projects}`}>{COMPANY_INFO.projects}</span>
            <dd class="stat__label">Proyectos Completados</dd>
          </dt>
          <dt class="stat">
            <span class="stat__number" aria-label={`${COMPANY_INFO.satisfaction} satisfacción`}>{COMPANY_INFO.satisfaction}</span>
            <dd class="stat__label">Satisfacción del Cliente</dd>
          </dt>
        </dl>
      </div>
    </div>
  </div>

  <style>
    .about {
      padding: var(--spacing-2xl) 0;
    }
    
    .about__content {
      display: grid;
      grid-template-columns: 1fr 1.5fr;
      gap: var(--spacing-xl);
      align-items: center;
      max-width: 900px;
      margin: 0 auto;
      position: relative;
      z-index: 2;
    }
    
    .about__image-wrapper {
      position: relative;
      border-radius: var(--border-radius-xl);
      overflow: hidden;
      box-shadow: var(--shadow-lg);
    }
    
    .about__img {
      margin: 0;
      position: relative;
      z-index: 2;
    }
    
    .about__img img {
      width: 100%;
      height: 350px;
      object-fit:contain;
      border-radius: var(--border-radius-xl);
      transition: transform var(--transition-slow);
    }
    
    .about__image-wrapper:hover .about__img img {
      transform: scale(1.05);
    }
    
    .about__image-overlay {
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(135deg, rgba(119, 188, 219, 0.1) 0%, rgba(41, 71, 88, 0.1) 100%);
      z-index: 1;
      border-radius: var(--border-radius-xl);
    }
    
    .about__text {
      display: flex;
      flex-direction: column;
      gap: var(--spacing-md);
    }
    
    .about__header {
      margin-bottom: var(--spacing-md);
    }
    
    .about__subtitle {
      display: inline-block;
      color: var(--color-primary);
      font-size: 0.875rem;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 1px;
      margin-bottom: var(--spacing-sm);
      background: rgba(119, 188, 219, 0.1);
      padding: 0.5rem 1rem;
      border-radius: var(--border-radius-sm);
    }
    
    .about__title {
      font-family: var(--font-display);
      font-size: clamp(1.75rem, 4vw, 2.5rem);
      font-weight: 700;
      line-height: 1.2;
      color: var(--color-neutral-900);
      margin: 0;
    }
    
    .about__description {
      display: flex;
      flex-direction: column;
      gap: var(--spacing-md);
    }
    
    .about__paragraph {
      font-size: 1.125rem;
      line-height: 1.7;
      color: var(--color-neutral-700);
      margin: 0;
    }
    
    .about__stats {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: var(--spacing-md);
      margin-top: var(--spacing-lg);
      padding-top: var(--spacing-lg);
      border-top: 1px solid var(--color-neutral-200);
    }
    
    .stat {
      text-align: center;
      padding: var(--spacing-md);
      background: var(--color-neutral-50);
      border-radius: var(--border-radius-lg);
      transition: all var(--transition-normal);
    }
    
    .stat:hover {
      transform: translateY(-2px);
      box-shadow: var(--shadow-md);
    }
    
    .stat__number {
      display: block;
      font-family: var(--font-display);
      font-size: 2rem;
      font-weight: 700;
      color: var(--color-primary);
      line-height: 1;
      margin-bottom: 0.5rem;
    }
    
    .stat__label {
      font-size: 0.875rem;
      font-weight: 500;
      color: var(--color-neutral-600);
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }
    
    @media (max-width: 900px) {
      .about__content {
        grid-template-columns: 1fr;
        gap: var(--spacing-xl);
        text-align: center;
      }
      
      .about__img img {
        height: 300px;
      }
      
      .about__stats {
        grid-template-columns: repeat(3, 1fr);
        gap: var(--spacing-sm);
      }
      
      .stat {
        padding: var(--spacing-sm);
      }
      
      .stat__number {
        font-size: 1.5rem;
      }
      
      .stat__label {
        font-size: 0.75rem;
      }
    }
    
    @media (max-width: 600px) {
      .about {
        padding: var(--spacing-2xl) 0;
      }
      
      .about__stats {
        grid-template-columns: 1fr;
        gap: var(--spacing-sm);
      }
      
      .about__img img {
        height: 250px;
      }
    }
  </style>
</section>